# Z-View 项目问题清单与修复跟踪

> 生成日期：2026-09-20
> 更新：2026-09-20 追加第五节（1.9.52→1.9.55 发布修复与新增）
> 状态说明：`[ ]` 待修复，`[x]` 已完成

---

## 一、明确的 Bug（高优先级）

### 1. WMI 采集线程未初始化 COM（Agent 端，高频报错）
- **状态**：[x] 已完成（2026-09-20）
- **修复内容**：`zvagent/collectors/system.py` 的 `_wmi_query` 函数中添加 `pythoncom.CoInitialize()` / `CoUninitialize()`，显式释放 COM 对象并调用 `gc.collect()`
- **验证**：Agent 重启后 `agent-worker.log` 中 CoInitialize 报错从 20+/30s 降为 0；WMI 查询在线程中正常返回硬件数据；单元测试 7/7 通过
- **位置**：`zvagent/collectors/system.py`
- **现象**：`agent-worker.log` 中每秒报 `尚未调用 CoInitialize` 错误，硬件/GPU/主板/BIOS 信息采集全部失败
- **原因**：`_wmi_query` 在后台线程中调用 `win32com.client.Dispatch`，未调用 `pythoncom.CoInitialize()`
- **影响**：资产硬件详情采集失效，GPU/主板/BIOS 字段为空
- **验证方法**：重启 Agent 后查看 `agent-worker.log`，确认无 CoInitialize 报错；数据库中 gpu_name / motherboard / bios_vendor 等字段有值

### 2. 私钥证书已入库（安全问题）
- **状态**：[x] 索引清理完成（2026-09-20）；[ ] 历史密钥轮换（替代历史重写）
- **位置**：`frontend/certs/zview-key.pem`、`wt_certs/wt-key.pem`、`wt_certs/zview-root-key.pem`
- **已完成**：
  - 所有私钥/证书从 git 索引移除（`git rm --cached`）
  - 补充遗漏：`signing/zview-codesign.pfx`（含私钥）曾仍在索引——已 `git rm --cached` 移除；
    修正 `.gitignore` 中 signing 目录规则路径层级错误（`IT2026/IT2026/IT2026/signing/` → `IT2026/IT2026/signing/`）
  - `.gitignore` 增加全局 `*-key.pem` / `*.key` / `*.p12` / `*.pfx` 规则
  - `.gitignore` 增加 `frontend/certs/` 和 `wt_certs/` 目录级忽略
  - 新增 `scripts/gen_frontend_cert.py` 证书生成脚本，新机器可自行生成
- **待处理（决策已定：轮换替代重写）**：
  - 历史提交中仍有私钥。重写 109 个 commit 成本高（需全员 force-push 重同步），
    改为「视为已泄露、执行密钥轮换」：前端自签证书用 `gen_frontend_cert.py` 重生成、
    wt_certs 内部根证书轮换、代码签名证书换新。轮换完成后历史中的旧私钥即失效
- **轮换台账（2026-09-20 执行）**：
  - ✅ 前端自签证书：`gen_frontend_cert.py` 重生成（新私钥已替换）
  - ✅ WT 叶子证书：删除 wt-cert/wt-key 后 `ensure_wt_cert()` 重签（同一根 CA，终端信任不受影响；叶子 14 天自动续期）——泄露的 wt-key.pem 作废
  - ✅ 代码签名证书：新自签 CodeSigning 证书（Thumbprint `2EE71F46...`，至 2029）入 CurrentUser\My；泄露身份的旧证书（`93C05132...`）已从存储移除；`sign_agent.ps1` 按 NotAfter 自动选用新证书；新证书未铺到终端时升级护栏按 NotTrusted 告警继续（不阻断升级）
  - ✅ **WT 根 CA 私钥：已轮换（2026-09-21，#2 收官）**——双信任窗口策略零中断执行：
    ① 归档泄露旧钥（`zview-root-key.pem.leaked-20260921.bak`，旧 cer 保留 `zview-root-old.cer`）
    ② 删除现有根文件 → `wt_cert.ensure_root_ca()` 自动生成新根（同 CN，新密钥对，
       指纹 `CD998EF4AC3AD1CD75E4726927D5AE1D8B9ABC29`，至 2036-09-18）
    ③ `ensure_wt_cert()` 检测 issuer 失配自动重签叶子（11 天自动续期不变）
    ④ 新根导入本机 LocalMachine\Root（浏览器观看端信任锚）；旧根过渡期保留
    ⑤ GPO 包副本 `GPO部署包/zview-root.cer` 已替换（deploy.bat 下次运行即导入新根）；
       build_agent.ps1 打包自动携带新根
    ⑥ WT 网关重启加载新叶（UDP 4433 监听恢复，0 活跃会话窗口执行，实测叶链至新根）
  - ⏳ **运维收尾（GPO 过渡）**：① 有观看端的其他机器需导入新根（GPO 下发或 certutil -addstore）；
    ② 过渡完成后从各端根存储移除旧根（指纹 `F3A232690B1D48EDB7ACB05E610ACD899F84A1DA`），
    移除后泄露旧钥彻底失效
  - ✅ **Agent TLS 通道根治（2026-09-22）**：前端证书轮换曾致 XXH-XXX 的 agent 卡 SSL 重试
    循环 47h（https 配置无自动协商回退，#4 的 fail_ttl 只重探测不降级）——根治：**8443 证书
    改由 WT 根 CA 签发**（SAN 平台 IP/localhost/主机名，至 2031），Agent 的 runtime/ca-bundle.pem
    切换为 WT 根证书（10 年稳定）——今后前端/8443 证书轮换对 Agent 透明。执行后本机心跳恢复
    （age 4s，[Heartbeat] OK 恢复），DESKTOP-JEGI046 已升级 1.9.56 并正常心跳
- **轮换后状态**：泄露私钥全部作废——前端自签证书 ✓、WT 叶子 ✓、代码签名 ✓、**WT 根 CA ✓**；
  唯一保留的旧信任为过渡窗口内的旧根证书本体（其私钥已归档隔离，不再存在于任何活文件）
- **验证**：`git ls-files | grep key.pem` 无输出；`git ls-files | grep .pfx` 无输出；
  `git check-ignore signing/zview-codesign.pfx` 命中 `*.pfx` 规则；磁盘证书保留，服务正常

### 4. Agent TLS 缓存钉死故障：服务端证书变更后 Agent 无法自愈
- **状态**：[x] 已完成（2026-09-20）
- **位置**：`zvagent/auth.py`（`_platform_base`）、`zvagent/heartbeat.py`
- **现象**：Agent 的 TLS 自动协商一旦探测 HTTPS 成功并缓存 `tls_ok=true`（24h），后续即使服务端证书变更导致 SSL 验证失败，Agent 也不会降级回 HTTP，会一直心跳失败直到自愈合退出重启
- **根本原因**：
  1. 正向缓存 24h 内直接用 HTTPS，不再探测
  2. 心跳 SSL 错误没有触发缓存失效
  3. 验证失败和连接失败的回退时间一样（10min），证书不匹配的场景重试没意义
- **修复内容**：
  1. `_platform_base()` 增加 `fail_ttl` 机制：SSL/cert 验证失败缓存 1h，连接失败缓存 10min
  2. 新增 `invalidate_tls_cache()` 函数：强制清除 TLS 缓存触发重新探测
  3. `get_asset_id_from_server()` 和主心跳循环的异常处理中：检测到 SSL 错误时调用 `invalidate_tls_cache()`
- **验证**：
  - `invalidate_tls_cache()` 正常删除缓存文件
  - 语法检查通过；Agent 认证单元测试 7/7 通过
  - DESKTOP-JEGI046 机器删除 TLS 缓存后已恢复上线

### 3. assets_api.py 与 zvplatform 循环依赖
- **状态**：[x] 已完成（2026-09-20）
- **位置**：`zvplatform/routers/agent_heartbeat.py`
- **修复内容**：
  - 新增 `zvplatform/repositories/asset_repository.py`：`fetch_asset_row`、`record_asset_changes`、`record_asset_change_entry`、`normalize_history_value`
  - 新增 `zvplatform/repositories/agent_credential_repository.py`：`ensure_agent_credentials_table`、`issue_agent_device_credential`、`verify_agent_device_credential`、`agent_version_tuple`
  - `agent_heartbeat.py` 不再 import assets_api，全部从 zvplatform 仓库模块导入
  - `assets_api.py` 中的旧函数保留作为兼容封装（不删，避免影响其他调用方）
- **验证**：
  - `python -c "from zvplatform.routers.agent_heartbeat import router"` 直接 import 成功（无循环）
  - `python -c "import assets_api"` 128 条路由全部加载成功
  - 单元测试 70 passed, 15 skipped
  - Agent 心跳正常，`[Heartbeat] OK (asset_id=2241)` 持续上报

---

## 二、架构不合理 / 技术债（中优先级）

> 编号说明：原编号与技术债区/Bug 区重复，本节自 #16 起编。

### 16. 模块迁移半途而废，代码双份维护（原 #4，与 #10 合并跟踪）
- **状态**：[ ]
- **位置**：`assets_api.py` vs `zvplatform/`；`cmdb_agent_core.py` vs `zvagent/`
- **现象**：核心功能同时存在于旧单体文件和新模块化包中，通过运行时注入互相调用
- **影响**：修改一处功能需同步改两处，容易不一致
- **进展**：#3 已把心跳链路的资产/凭据访问迁入 `zvplatform/repositories/`（首个落地片段）

### 17. 配置入口太多太乱（原 #5）
- **状态**：[x] 两阶段完成（2026-09-21）：①配置来源可追踪 ②入口审计与重新归类；
  剩余"入口文件数物理精简"（如 auth_state 入库）列 #10 专项
- **痛点回顾**：后端 .env + config.json + config.local.json + auth_state.json + auth_secret.txt + 环境变量 + 代码默认值；难以追踪配置项最终值来源
- **第一阶段（来源可追踪，2026-09-21）**：
  - `config_utils.py`：.env 加载记录来源（`_ENV_FILE_SOURCES`/`get_env_files_loaded`/`describe_env_source`），
    语义不变（setdefault → 显式判断）
  - 平台：新增 `zvplatform/routers/config_report.py` —— GET `/api/v1/console/config-report`（admin，auth:manage），
    每个配置项 {value(敏感项脱敏), env, source(默认值/环境变量/.env(路径))} + secrets 来源（TOKEN_SECRET/
    agent token 的 env/文件/自动生成判定）+ 文件清单（实际加载的 .env、auth_secret、agent_token、auth_state）
  - Agent：新增 `--print-config`（JSON 输出最终生效配置脱敏值 + 来源标签：配置文件(路径)/环境变量/默认值，
    含 active_config_file 与候选清单），`zvagent/config.build_config_report()` 承载
  - **验证**：config-report 真实 HTTPS 200（六组配置 + secrets 来源判定"自动生成并落盘"正确 + .env 双文件加载记录）、
    agent --print-config 实测（token 脱敏、server_url 来源=环境变量）、回归 18/18×2+4/4、stderr 零 Traceback
- **第二阶段（入口审计与重新归类，2026-09-21）**：
  - 审计结论：**平台配置入口已是最小集**——.env 9 键全部被消费、auth_secret.txt 为密钥持久化、
    环境变量与代码默认值（settings.py 集中）均为必要层次；**无残留死入口**
  - 重新归类（非平台配置，从配置入口除名）：auth_state.json=运行时状态（用户账号，入库列 #10）、
    agent_secret.txt=密钥文件、config.json/config.local.json=Agent 配置（平台侧零消费经全量核查；
    config.json 已被 gitignore 未入库）
  - config-report 端点 files 段升级为 entry_audit 分类报告（平台配置/密钥文件/运行时状态/Agent 配置）
  - ✅ **auth_state 已入库（2026-09-22，专项 1）**：用户账号迁入 `platform_users` 表
    （MySQL，字段一一对应含 scoped_group_ids JSON）；auth_utils 持久层 DB 优先 +
    auth_state.json 文件回落双写（MySQL 闪断时登录韧性不变，文件保留为回落快照）；
    首次加载自动迁移；写路径双写实测（启停往返 DB/文件同步）；
    登录链路实测（错密码 401 走 DB hash 校验 / me / users 200）；
    回归五套全绿。auth_state.json 重新归类为「回落快照（非配置）」
- **验证**：端点实测 entry_audit 四类归类正确、config.json 零消费结论经全量 grep 复核、回归 18/18×2+4/4

### 18. 三个独立后端服务 + 反向代理，部署复杂（原 #6）
- **状态**：[x] 收官（2026-09-22，四批次全部交付；服务收敛为三进程一键运维）
- **拓扑现状（2026-09-21 核实）**：
  - 主服务 8080/8443（assets_api，含 Agent 软件通道反代 `/api/v1/software/agent` → 8081）
  - 软件管理 8081（software_management_api_complete_v2.py，3071 行：包管理/分发任务）
  - 软件策略 8082（software_policy_api.py，1475 行：强制安装策略）
  - WT 网关 UDP 4433（webtransport_gateway.py，浏览器观看端 WebTransport）
  - 前端三路代理：vite `/api/v1/software/*`(除 all/stats)→8081、`/software-api`→8081、`/policy-api`→8082
  - Agent 双 URL：主服务 8443 + `ZVIEW_SOFTWARE_SERVER_URL`→8081
- **数据核实**：软件功能真实在用（24 包/22 任务/2250 策略日志），四个服务全部在线（2026-09-21 曾误判 8081/8082 下线，系 netstat 解析错误，已修正）
- **批次 A（已交付，2026-09-21）**：
  - 新增 `zvplatform/routers/software_management.py`：software_management_api_complete_v2.py
    （3071 行，40 端点：包 CRUD/上传/下载/分类/统计、任务 CRUD/poll/cancel/retry、
    agent policies/poll/download/task-results/logs、whitelist CRUD、software policies、compliance 全套）
    逐字迁入主服务；鉴权与主服务同源（auth_middleware 按 path 强制 + Agent 端点豁免并自带
    require_agent_request，主 AUTH_EXEMPTIONS 已补 4 条）
  - Agent 反向代理路由退役（同路径直连）；DB 连接统一 zvplatform.db（同库同时区行为一致）
  - **零停机**：8081 独立进程兼容期保留（Agent 的 ZVIEW_SOFTWARE_SERVER_URL 批次 C 再切）
- **验证（批次 A）**：40 条路由零重复、py_compile 通过、真实 HTTPS 四类实测全 200
  （packages/health/stats/agent poll）、回归 18/18×2+4/4+15/15+6/6、stderr 零应用 Traceback
  （仅 Windows asyncio 连接重置噪音）
- **批次 B（已交付，2026-09-21）**：
  - 新增 `zvplatform/routers/software_policy.py`：software_policy_api.py（1475 行，10 端点：
    策略 CRUD/执行/日志/check 收敛检查）逐字迁入；`init_database` 建表随主服务导入执行一次
    （原 8082 on_event 等价）；`enforce_force_install_convergence` 随迁，
    software_management 的 poll_tasks 引用改指新模块
  - 主 AUTH_EXEMPTIONS 补 `/api/v1/policies/check/*` GET（Agent 收敛检查豁免）
  - 补齐迁移缺失名：AGENT_INSTALL_STATUS_INSTALLED（constants）、
    POLICY_TARGET_ONLINE_SECONDS（8082 模块级常量随迁）、get_db_connection 别名与裸名双导入
- **验证（批次 B）**：10 条路由零重复、真实 HTTPS 实测 policies 200(2 条策略)/check 200
  (allowed=true 收敛判定正常)/agent poll 200（走新模块收敛链）/packages 200、
  回归五套全绿、stderr 零应用 Traceback
- **批次 C+D（已交付，2026-09-22，#18 收官）**：
  - 批次 C：Agent 软件通道正式切主服务——核实终端 `config.local.json` 的
    `software_server_url` 派生结果本就是主服务 URL（8443 部署不含 :8080，replace 不触发）；
    vite.config.mjs 三路代理目标 8081/8082 → 8080（保留 VITE_PROXY_* 覆盖能力以便回滚）；
    8081/8082 独立进程退役（端口零监听）
  - 批次 D：新增 `scripts/start_all.ps1` / `stop_all.ps1` 一键启停（主服务+WT 网关+前端
    preview 三服务，进程名+命令行特征精确检测防重复启动，日志统一 logs\ 目录）；
    停止→启动全循环验证通过
- **验证（批次 C+D）**：preview 经新代理 software-api/policy-api/software/all 全 200、
  agent poll 200、UDP 4433 由网关绑定（Get-NetUDPEndpoint 权威确认）、
  四端口（8080/8443/4173/4433-udp）全 UP、stop→start 循环后全栈 200
- **批次 9（WS 基建整合，2026-09-22，#16 端点迁移真正收官）**：
  - 新增 `zvplatform/routers/remote_desktop_ws.py`：两个 remote-desktop/remote-session
    websocket 路由（312 行）逐字迁入；WS 会话基建（会话管理器/桥接/审计助手共 13 名）
    经 `bind_remote_ws_helpers` 注入共享引用（可变状态按引用共享，行为不变）；
    WebSocketState 导入对齐 assets_api（starlette.websockets）
  - **WS 握手级冒烟验证**（补齐此前"WS 无法 CLI 验证"的缺口）：
    无令牌 → HTTP 403（鉴权生效）；有令牌 → 握手成功 + 结构化 session_error 帧
    （处理器执行到目标检查阶段，XXH-XXX 心跳离线为真实数据态）；坏会话 ID → 403
    （原语义逐字保留）。数据流（媒体帧中继）无法 CLI 验证，代码逐字未改、风险限于模块解析层
- **#16 状态**：[x] 端点迁移真正收官——assets_api 从 4887 行降至 **1846 行（-62%）**，
  仅剩应用中间件、后台 worker、/metrics、/api/health、根路径（应用级基建，有意保留）
- **#18 状态**：[x] 收官——四服务收敛为主服务(8080/8443)+WT网关(4433-udp)+前端(4173)三进程，
  start_all/stop_all 一键运维；剩余仅为可选的 WS 基建整合（websocket 暂留 assets_api）

### 19. .gitignore 混乱且重复（原 #7）
- **状态**：[x] 已完成（2026-09-20）
- **现象**：94 行规则，多层层级重复，中英文注释混杂
- **修复内容**：
  - 从 94 行精简
  - 按类别分组：Python 缓存 / 前端构建 / 日志临时文件 / IDE 状态 / 构建产物 / 密钥配置
  - 移除所有重复规则（如 `IT2026/.env` 与 `IT2026/IT2026/.env`）
  - 全局规则统一放前面，项目特定路径放后面
  - 补充 `config.json` 全局忽略（原来只按路径忽略，容易漏）
  - 修正 signing 目录规则路径层级（见 #2）
- **验证**：
  - 所有原被忽略的文件仍然被忽略
  - 所有源码文件仍然被追踪
  - `git status` 无意外变更

### 20. 项目目录嵌套太深（原 #8）
- **状态**：[x] 盘点与清理完成（2026-09-21）；目录重命名经评估**决定保留**
- **结构说明（2026-09-21 核实）**：
  - `D:\IT2026\IT2026` = git 仓库根（.git 911MB）
  - `D:\IT2026\IT2026\IT2026` = 过渡层：曾为项目旧位置，残留已清理（见下），
    现仅剩工具状态目录（.kilo/.claude）
  - `D:\IT2026\IT2026\IT2026\IT2026` = 项目本体（全部源码/构建/发布）
- **已清理（2026-09-21）**：
  - 三层残留：`.playwright-mcp`（50 文件）、`.ruff_cache`、`kilo_tmp`、
    `Drivers`（残缺副本，完整版在四层）、`frontend`（旧副本，ComplianceManagement.vue 四层仍在）
  - 三层 `文档/` 9 个独有设计文档 **git mv** 至四层 `docs/archive-design/`（保留历史）
  - 最外层 `D:\IT2026\backups`（2.5GB：09-08 时点备份 + cmdb-python-dump.sql）压缩为
    `zview-backup-20260908-archive.zip`（2.3GB，48 条目完整性校验通过）后删除原目录；
    `diagnostics`（0.2MB）保留
- **重命名评估结论（保留嵌套）**：移动项目目录需停全部服务（4 进程）+ 迁移后服务 cwd/脚本/
  GPO 引用全量更新 + git 全树路径前缀变化（历史对比/回滚全乱），收益纯美观、风险覆盖生产链路；
  三层遗迹已清空后，实际困惑点已消除。如确需扁平化，按维护窗口 runbook 单独执行

---

## 三、未完成功能（低优先级，需产品决策）

### 9. P1 批次 1 功能待开发
- **状态**：[x] 全部完成（2026-09-21 复核后全部闭环/移出）
- 逐项结论：
  - GPU/主板/BIOS 字段：随 #1 CoInitialize 修复上线（已移出）
  - Remote Shell：1.9.35 完成并两轮迭代（已移出）
  - **升级失败自动重试：已实现**——`zvagent/upgrade.py` 自 2026-09-09 事故后即有
    断路器 + 24h 滚动窗口失败计数 + 指数退避（next_retry_at），本复核确认后闭环
  - **Fleet 健康度：已实现**——GET `/api/v1/agent-fleet/health`（#16 第六批迁入
    `zvplatform/routers/assets.py`）+ 前端 `AgentFleetHealth.vue` 已调用，本复核确认后闭环
  - **资产自定义字段：本批交付**——assets.custom_fields（MySQL JSON 列，已存在）端到端打通：
    创建/更新端点支持（dict 校验 + json.dumps + 变更历史进 diff）、单资产/详情返回解析为对象、
    前端 AssetInfoPanel 展示卡 + 编辑态键值对动态编辑（随保存提交）；实测写入中文键值
    →回读→清空全通过
- 前端验证：`npm run build` 通过；自定义字段经真实 HTTPS PUT/GET 往返验证

### 10. zvplatform 分层迁移未完成
- **状态**：[ ]
- repositories/ 缺资产/软件/Agent；services/ 缺对应服务；核心 DB 连接仍在 assets_api.py

### 11. 资产发现前端未产品化（65%）
- **状态**：[ ]

### 12. 安全模块前端与后端不对齐
- **状态**：[x] 对齐完成（2026-09-21，24 端点全部接线或评估）；展示层已完成第一轮迭代
- 后端：security_api.py 单体已整体迁入 `zvplatform/routers/security.py`（24 端点，见 #16 第五批）；
  `/overview` 扩展返回 usb(设备数/24h 事件)、executions(24h 总数/失败)、policies.firewall、
  recent_usb_events(Top10)、recent_failed(Top5)（注意 usb_events 真实列名为 occurred_at/friendly_name/vid_pid）
- 前端对齐内容（2026-09-20~21）：
  - `api/security.js` 补齐 `remoteUnisolate`（远程解除隔离此前无 API 封装）
  - 终端安全详情页新增「远程安全运维」卡片：安全扫描/结束进程（PID 或进程名）/隔离终端/解除隔离，
    带确认弹窗、下发结果回显（写审计日志走后端既有链路）
  - 策略中心新增「新建策略」对话框（名称/类型/优先级/描述/配置 JSON，JSON 合法性校验）
  - 策略绑定对话框新增「现有绑定」列表 + 解绑操作（getSecurityPolicyDetail → bindings）
  - 防火墙页新增「终端防火墙状态」区（按终端查询最近 5 次策略下发结果，getFirewallStatus 已接线）
  - 总览页升级为态势看板：四维统计卡（终端/策略/USB/24h 执行含失败警示色）+
    最近 USB 事件表 + 最近失败策略执行表 + 子页快捷入口

---

## 四、较小的问题

### 13. 根目录空文件与临时文件
- **状态**：[x] 已完成（2026-09-20）
- **修复内容**：
  - `findings.md`（0 字节）从 git 移除，加入 `.gitignore`
  - `.playwright-mcp/`（30+ 个临时 yml 文件）从 git 移除，加入 `.gitignore`
  - `frontend-dev.log` 已被 `*.log` 规则覆盖
- **验证**：`git ls-files findings.md` 无输出；`git ls-files | grep playwright-mcp` 无输出
### 14. 数据库表命名风格不统一
- **状态**：[x] 已完成（2026-09-20）
- **盘点结果**：全库 63 张表，仅 `network_interfaces_legacy_20260609` 一张遗留表且 0 行数据
- **修复内容**：确认空表后 `DROP`，全库 62 张表，已无 legacy/日期后缀遗留表
- **验证**：`SHOW TABLES` 复查无 legacy/`_2026`/`_old`/`_bak`/tmp/_copy 命名

### 15. 测试覆盖率不均
- **状态**：[x] 原缺口全部补齐（2026-09-21，五套 61 用例全绿）
- **测试清单**（全部全离线桩，不触真实 DB/配置；运行方式 `python tests/test_*.py`）：
  - `test_agent_exit_policy.py`（18/18）：退出密码策略 RBAC 映射、启用/停用落库（sha256）、
    Agent 校验端点放行/拒绝/fail-closed
  - `test_auth_tokens.py`（18/18）：访问令牌签发/校验往返、篡改/空/垃圾令牌拒绝、
    关键路径权限映射、角色权限模板（viewer/operator）
  - `test_agent_config_bom.py`（4/4）：Agent 配置加载 BOM 兼容/坏 JSON 静默回退/缺文件
  - `test_assets_crud.py`（15/15）：资产列表结构/日期格式化/实时状态、custom_fields 读写
    （JSON 往返、非法入参 422）、创建/更新变更历史 diff、非法 asset_type 拒绝
  - `test_software_records.py`（6/6）：软件全量/按终端过滤/stats Top N
- **待补（新功能随手补）**：后续新端点沿用 bind_helpers/桩游标模式补测试

---

## 五、发布修复与新增（1.9.52→1.9.55，2026-09-18~20，全部完成）

### 1.9.52 坏版本事故：构建缺 `__version__`，全员 Agent 启动即崩
- **状态**：[x] 已完成（1.9.53 修复发布）
- **现象**：`zvagent/__init__.py` 中 `__version__ = "1.9.52"` 与注释粘成一行，名字未定义；所有 1.9.52 终端的用户会话 Agent 启动即抛 `ImportError: cannot import name '__version__'`
- **修复**：重写 `__init__.py`（版本号独立成行）→ 1.9.53 重建发布，updater 自动滚动恢复（服务端机器 1 分钟自愈）
- **预防**：发布前必须 `python -c "import zvagent; print(zvagent.__version__)"` 自检；该文件曾是 GBK/UTF-8 双重乱码，手工编辑易吞换行
- **位置**：`zvagent/__init__.py`

### 托盘「退出代理」假退出（控制台永远在线）
- **状态**：[x] 已完成（1.9.54）
- **现象**：右键退出只 `DestroyWindow` 杀托盘自身，服务/会话进程照常心跳 → 控制台永远 online
- **修复**：确认框 → UAC 提权拉起 `--stop-agent`（sc stop + 等真正 STOPPED + 清会话残留）；心跳停止后控制台 90s 内离线；UAC 取消则托盘保持驻留
- **位置**：`cmdb_agent_consent_ui.py`、`cmdb_agent_unified_v2.run_stop_agent`

### Agent 自安装：对运行中目录重装即损坏 + `sc start` 阻塞崩溃
- **状态**：[x] 已完成（1.9.53/1.9.54）
- **现象**：同版本重装对运行中的 `versions\<ver>` 做 rmtree+copytree → `_internal` 拷坏（jaraco `Lorem ipsum.txt` 缺失）→ 服务 import 断裂、永远不上报 RUNNING（SCM StartPending 挂死）；另 `sc start` 阻塞 120s 的 TimeoutExpired 未捕获，直接崩掉整个自安装
- **修复**：同版本已存在则跳过拷贝（幂等）；`_run` 超时/异常硬化（返回码 124，不再抛栈）；net stop 后轮询等真正 STOPPED；sc start 后轮询 RUNNING 90s 才算成功
- **位置**：`cmdb_agent_unified_v2.run_self_install`

### 管理台安装包下载链路三连修
- **状态**：[x] 已完成
- 内容：① 下载文件名 .exe→.zip + onedir-zip 回退（0e9e7141/c498f9ea/19075e40）② 服务端 `os` 未导入导致下载 500 ③ axios 全局 30s 超时掐断 75MB+ 大包下载（改 timeout:0），文件名由服务端 `X-Agent-Package-Filename` 响应头下发
- **位置**：`assets_api.py`、`frontend/src/views/terminal/AgentDeploy.vue`

### 新增：图形化安装向导（双击 → 下一步 → 完成）
- **状态**：[x] 已完成（1.9.53 起随包）
- 内容：`setup_wizard.py`（Tk 向导、UAC 提权、安装进度页、静默模式 `/S --server-url=`、自检 `--dry-run`、日志 `%TEMP%\zview-setup.log`）；`scripts/build_setup_exe.py <ver> --publish` 构建（内嵌 onedir zip 为 payload.zip + setup_meta.json 默认服务器地址）；控制台「下载安装包」优先下发 `Z-View-Setup-<ver>.exe`
- **验证**：`--dry-run` 自检通过 + 静默模式全链路实测 exit=0；本机重装与新装均验证

### 新增：Agent 退出密码策略
- **状态**：[x] 已完成（1.9.55）
- 内容：服务端 `zvplatform/routers/agent_exit_policy.py`（管理端 GET/PUT `/api/v1/console/agent-exit-policy`，sha256 存 `system_config`，明文不落库；Agent 端 POST `/api/v1/agent/exit/verify`，未设密码放行、管理台不可达 fail-closed）；管理台 Agent 部署页「退出密码」卡片；托盘退出流程接入密码验证
- **验证**：真实 HTTPS 全链路（管理端 token 签发实测）+ 回归测试 18/18；期间 500 系 `config_type='security'` 超出 ENUM 取值（改 'string'）+ 后端未重启跑旧代码，均已修复

### 事件：f55a109b 合并遗留冲突标记入库（2026-09-20，已修复）
- **现象**：merge 时 `assets_api.py` / `AgentDeploy.vue` / `zvagent/__init__.py` 三处冲突标记（`<<<<<<< HEAD`）未解决即提交——assets_api 冲突块是非法 Python 语法，在跑的后端靠内存旧代码幸存，**下次重启即崩**
- **修复**：`9aecec62` 三处均保留 HEAD（1.9.55 最新实现）；`git grep` 复查全库零冲突标记
- **预防**：merge 后必须 `git grep '<<<<<<< '` 再提交；py_compile 通过才允许重启服务

### 16. 模块迁移进展（2026-09-20）
- 第一批：`zvplatform/routers/agent_deploy.py`——终端部署三件套（安装包下载/部署脚本生成）自 assets_api 迁入，
  assets_api 旧实现删除、路由挂载（`include_router`），部署解析逻辑（setup exe 优先/zip 回退）原样保留
- 第二批：`zvplatform/routers/alert_settings.py`（告警通知 GET/PUT/试发 + 阈值 GET/PUT）、
  `zvplatform/routers/agent_credentials.py`（凭据列表 + 吊销含审计）自 assets_api 迁入，
  权限语义不变（中间件按 path 解析，与处理位置无关）
- 第三批：`zvplatform/routers/patch_management.py`——补丁状态列表（GET，可按终端过滤，含 agent_patches 表自建）迁入
- 第四批：`zvplatform/routers/agent_security_policies.py`——agent 面 security-policies（三级优先级解析）
  与 security-policy-result（执行结果回传）迁入；`_enforce_device_asset_binding` 一并迁移（设备凭据仅访问自身资产）
- 加固：Agent 配置读取统一 `utf-8-sig`（config.py/agent_consent_ipc/unified_v2）——PowerShell 等工具
  重写 config.local.json 引入 BOM，旧加载器遇 BOM 异常静默回退空配置（agent 重启丢 server_url/token 离线循环）；
  磁盘 BOM 已剥离，新增 `tests/test_agent_config_bom.py`（4/4）
- 第五批：**security_api.py 单体（1010 行，24 端点）整体迁入 `zvplatform/routers/security.py`**
  （#12 整合的第一步）——内容逐字保留，仅 DB 接入统一走 zvplatform.db；security_api.py 转为兼容垫片
  （re-export router/ensure_security_tables/mount_security_api）；全部 5 处引用方
  （assets_api 启动表自建+挂载、agent_security_policies×2、policy_registry）改指新模块
- 第六批：**资产 CRUD 主体迁入 `zvplatform/routers/assets.py`**（21 条路由：stats/list/options/export/
  detail/create/update/delete/batch-delete/lifecycle/status/uptime/changes/command/trigger-report，
  含 Asset 系列模型类；两个 remote-desktop websocket 依赖模块内 WS 基建，暂留 assets_api）。
  共享助手（get_asset_agent_target/proxy_agent_json_request/record_system_activity_log/
  ensure_asset_changes_table/AGENT_CONTROL_PORT）经 `bind_helpers` 注入（assets_api 尾部调用，
  调用期解析，避免循环导入）；后续批次逐步下沉 repositories
- 第七批：**认证与用户管理迁入 `zvplatform/routers/auth.py`**（login 免鉴权/me/change-password/
  users CRUD/重置密码，含 LoginRequest 等模型与 _log_user_management 审计助手；
  record_system_activity_log 经 bind 注入）；权限语义不变（中间件按 path 强制 auth:manage）
- 第八批（收官）：`zvplatform/routers/software.py`（软件安装记录查询 2 端点迁入）；
  **清理死代码约 470 行**——discovery 助手全套（create/upsert/ping 等 16 个函数，活实现已在
  `zvplatform/services/discovery_service.py`）与孤儿 `start_ping_discovery`（无装饰器，
  活端点在 discovery 路由），零外部引用经全量核查后删除
- **#16 端点迁移收官**：assets_api 从 4887 行降至约 2270 行（-54%）。显式保留项：
  ① 两个 remote-desktop websocket（依赖模块内 WS 基建，且 WS 会话行为无法在 CLI 充分验证，
    迁移风险大于收益）② /metrics、/api/health、/ 根路径与应用中间件、后台 worker（应用级基建）
  ③ Agent 端 cmdb_agent_core.py → zvagent 委托（1006 行实逻辑，单列专项）
- 加固：Agent 配置读取统一 `utf-8-sig`（config.py/agent_consent_ipc/unified_v2）——PowerShell 等工具
  重写 config.local.json 引入 BOM，旧加载器遇 BOM 异常静默回退空配置（agent 重启丢 server_url/token 离线循环）；
  磁盘 BOM 已剥离，新增 `tests/test_agent_config_bom.py`（4/4）
- 验证：每批均路由注册无重复、py_compile 通过、真实 HTTPS 实测（package 200/100MB/文件名头、
  script 200、credentials 200(2 台已注册)、thresholds/notify-config 200、patch-status 200(2 台 3 补丁)、
  security 401/overview 200/policies 200/terminals 200、资产 8 端点全 200(stats/list/options/detail/
  status/uptime/changes(57190 条历史)/detail/export CSV)、auth 401×2/me 200/users 200（登录审计落库）、
  software 200×2/health 200、回归 18/18×2+4/4、后端重启正常
- 下一步候选：#12 前端 security 页面对齐（端点已就绪）；assets_api 主体资产 CRUD 与
  Agent 端 cmdb_agent_core → zvagent 委托层迁移按专项规划

---

## 六、专项 2：agent core → zvagent 委托化（已交付，2026-09-22，1.9.56）

> 现状：cmdb_agent_core.py **1198 → 227 行（-81%）**，自有逻辑约 1000 行全部迁入 zvagent 五模块。
> 模式：与平台侧 #16 相同——逐字迁入 + core 留薄委托（re-export）+ frozen 打包实测。
> 验证：py_compile 六文件、import cmdb_agent_core 全量委托归属正确（四类实例状态归位）、
> 纯函数冒烟（parse_target_ids/_clamp_timeout/_patch_payload_hash）、
> **frozen 1.9.56 发布 → DESKTOP-JEGI046 自动升级并正常心跳（生产实测）**。

### 交付模块（按域分组，源行号为迁移前位置）
> **部署终态（2026-09-22 13:41）**：双终端全部升级 1.9.56 并正常心跳
> （DESKTOP-JEGI046 经 updater 自动升级；XXH-XXX 经手动 junction 部署——
> 其 updater 因证书修复前积累的升级状态滞留未自动触发，属 1.9.55 旧进程内状态，
> 新版 agent 升级流已含 STALE 处理）；1.9.56 服务启动日志零错误。
1. `zvagent/software.py`：`SoftwareManager` 类（223-553，约 330 行，软件安装执行器）+
   `start_software_management`（973）+ `_software_manager_instance` 声明
2. `zvagent/patches_report.py`：`_patch_payload_hash`（157）+ `_patch_report_loop`（172）+
   `_PATCH_FULL_SYNC_INTERVAL`/`_PATCH_REPORT_STATE` 常量随迁（补丁上报循环）
3. `zvagent/control.py`：控制命令执行群 `_truncate_control_output`(554)/
   `_raw_command_allowed`(574)/`_clamp_timeout`(578)/`_dispatch_zview_cmd`(586)/
   `handle_control_command_payload`(630)/`execute_control_command`(645)/`_job_command_handler`(697)
   + `AgentControlRequestHandler`(707)/`AgentControlServer`(782)/`StarletteWebSocketAdapter`(809)
   （Agent 控制通道 HTTP/WS 服务，端口 9001）
4. `zvagent/remote.py`（新）：`RemoteDesktopServer`(832-950) + `_server_instance` 声明 +
   `start_remote_desktop_server`(1169)
   （Python 侧远程桌面服务；C++ 捕获在 RemoteService/ 目录不动）
5. `zvagent/policy.py`（追加）：`SecurityPolicySync`(985-1100) + `apply_agent_firewall_whitelist`(1101)
   + `start_security_policy_sync`(1160) + `_security_policy_sync_instance` 声明
6. core 保留：`start_cmdb_reporter`(951)/`start_agent_control_server`(1185) 等装配入口 +
   全部 re-export（兼容 frozen 打包入口与存量导入）

### 执行要点（已验证）
- 逐字迁移 + core 尾部 re-export；`_AGENT_STATE/_LOCK` 共享状态直连 zvagent/state；
  跨模块实例读取经模块属性动态访问（from-import 是绑定快照，勿用于可变全局）
- 每批验证：py_compile + `import cmdb_agent_core`（全量委托归属正确）+
  单元级纯函数冒烟 + **frozen 1.9.56 → 测试终端自动升级实测（心跳正常）**
- 风险控制：控制通道（9001）与软件安装为生产路径；`SoftwareManager` 线程与子进程管理
  随模块迁移，实例声明跟随创建者函数所在模块
