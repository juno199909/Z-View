Z-View 最终产品定位

Z-View：企业 IT 终端与远程运维管理平台

英文：

Z-View Enterprise Endpoint & Remote Operations Platform

核心对象只有一个：

企业终端设备（Endpoint）

Agent 是执行核心，Web 控制台是管理入口。

一、最终产品架构
                         Z-View
                           │
             企业 IT 终端与远程运维管理平台
                           │
      ┌────────────────────┼────────────────────┐
      │                    │                    │
   资产管理              终端管理             远程运维
      │                    │                    │
    CMDB                Agent Fleet        Remote Desktop
    硬件                  心跳/状态          Remote Shell
    软件                  版本管理           File Transfer
    用户                  在线管理           Clipboard
    IP/MAC                批量管理           UAC
    生命周期              Agent升级          多会话
      │                    │                    │
      └────────────────────┼────────────────────┘
                           │
             ┌─────────────┴─────────────┐
             │                           │
          软件管理                    安全管理
             │                           │
         软件资产                    Defender状态
         软件仓库                    Firewall
         软件安装                    USB
         软件卸载                    Process
         软件升级                    FIM
         软件策略                    隔离
             │                           │
             └─────────────┬─────────────┘
                           │
                     Policy Engine
                           │
                  Automation / Job
                           │
                        Audit
二、① 资产管理 CMDB

这是整个系统的基础。

必做
设备资产
设备编号
设备名称
用户
部门
操作系统
CPU
内存
磁盘
GPU
主板/BIOS
IP
MAC
hostname
软件资产
Agent 信息
在线状态
首次发现
最后在线
资产生命周期
后续增强
资产标签
自定义字段
资产分组
部门组织树
资产导入/导出
资产变更记录
资产生命周期
三、② Agent Fleet 管理 ⭐⭐⭐⭐⭐

这是我认为你现在最应该重点补的一块。

现在你已经有 Agent、心跳和自动升级，但需要把它正式做成 Fleet。

Agent Fleet
├── 全部 Agent
├── 在线
├── 离线
├── 异常
├── 版本
├── 升级状态
├── 策略状态
├── 最后心跳
└── 健康度

Dashboard：

Agent 总数        84
在线              79
离线               5
版本过旧           8
升级失败           1
策略异常           3
必须支持
单 Agent 操作
批量操作
Agent 版本管理
自动升级
升级失败重试
升级回滚
Agent 健康状态
Agent 最后心跳
Agent 配置状态
Agent 与服务器连接状态
四、③ 远程运维 ⭐⭐⭐⭐⭐

这是 Z-View 的核心竞争力之一。

你已经投入很多，所以这里应该做深，而不是盲目增加协议数量。

Remote Desktop

继续完善：

WebTransport / QUIC
WebSocket fallback
H.264
GPU Encoder
JPEG fallback
QoS
动态分辨率
FPS
ACK
输入延迟
多显示器
DPI
全屏
Ctrl+Alt+Del
Windows 快捷键
UAC
Secure Desktop
Session 切换
重点任务

你刚才提到的：

远程桌面 FPS 低、操作卡顿

我会直接列为 P0/P1 技术任务。

最终应该做到：

Capture
 ↓
Encode
 ↓
Bounded Queue
 ↓
Transport
 ↓
Decode
 ↓
Render

并且独立监测：

Capture FPS
Encode FPS
Send FPS
Receive FPS
Decode FPS
Render FPS
RTT
ACK
Inflight
Input Latency
五、④ Remote Shell ⭐⭐⭐⭐⭐

这个我认为非常值得加。

远程桌面适合：

看问题、操作 GUI。

Remote Shell 适合：

真正的 IT 运维。

Windows：

PowerShell
CMD

以后 Linux：

Bash

需要：

实时输出
命令超时
Ctrl+C
工作目录
返回码
历史
权限控制
审计

例如：

管理员
 ↓
PC-001
 ↓
PowerShell
 ↓
Get-Service
 ↓
执行
 ↓
结果
 ↓
Audit
六、⑤ 软件管理 ⭐⭐⭐⭐⭐

这个也是非常核心的能力。

Software Management
├── 软件资产
├── 软件目录
├── 软件仓库
├── 安装
├── 卸载
├── 升级
├── 软件版本
├── 黑名单
├── 白名单
└── 软件策略

软件包需要记录：

名称
版本
架构
安装包
SHA256
安装命令
卸载命令
检测规则
依赖

最终实现：

选择 300 台电脑 → 部署软件 → 自动验证 → 失败重试 → 生成报告

七、⑥ Patch Management ⭐⭐⭐⭐⭐

这个建议加入。

但不是自己做补丁服务器。

主要管理：

Windows Update 状态

例如：

PC-001

Windows Update
├── 已安装 KB
├── 缺失 KB
├── 安全更新
├── Critical
├── Important
├── 重启要求
└── 最后更新时间

最终：

发现缺失补丁
 ↓
生成 Job
 ↓
批量修复
 ↓
重启
 ↓
验证
八、⑦ 终端安全管理 ⭐⭐⭐⭐

这里我们已经确定：

❌ 不做
自己维护病毒库
自研杀毒引擎
自研 AV
自研完整 EDR
沙箱
病毒特征检测

这些不要碰。

✅ 做 Endpoint Security Management
Windows Defender

读取：

是否启用
实时防护
病毒库版本
引擎版本
最后更新
最后扫描
Threat 状态
Firewall

你已有：

查询
开关
规则管理
策略

继续完善即可。

USB

你现在的 USBSTOR 控制可以继续：

允许
禁止
按资产
按用户/组
按策略
Process

继续：

黑名单
终止
策略
审计

但不要把它包装成“杀毒”。

FIM

继续：

文件基线
文件变化
Hash
告警

以后可以增加 SHA256，而不是只依赖 MD5。

Endpoint Isolation

保留。

这是很有价值的应急响应能力：

发现风险
 ↓
隔离终端
 ↓
仍保持 Z-View 管理通道
 ↓
远程处理
 ↓
恢复网络
九、⑧ Policy Engine ⭐⭐⭐⭐⭐

这个我非常建议你正式抽出来。

现在你的：

Security Policy
Software Policy
Agent Policy
USB Policy
Firewall Policy

不要各写一套。

统一成：

Policy Engine

模型：

Global
  ↓
Group
  ↓
Asset
  ↓
Priority
  ↓
Effective Policy

需要支持：

优先级
继承
覆盖
生效时间
版本
Revision
回滚
Dry Run
审计

最终：

任何终端配置为什么是这个状态？

系统都能回答。

十、⑨ Job / Task Center ⭐⭐⭐⭐⭐

这个也是必须补的。

把各种执行动作统一起来：

Job
├── 软件安装
├── 软件卸载
├── Agent升级
├── PowerShell
├── 安全策略
├── USB策略
├── Firewall
├── 系统配置
└── 批量操作

每个 Job：

任务
目标
创建人
创建时间
开始时间
结束时间
状态
成功
失败
超时
取消
重试
日志

状态机：

PENDING
 ↓
QUEUED
 ↓
RUNNING
 ↓
SUCCESS
 ↓
FAILED
 ↓
RETRY

这样你的平台会一下子“平台化”。

十一、⑩ Automation 自动化运维 ⭐⭐⭐⭐

Job 做好以后自然升级成 Automation。

例如：

IF
磁盘空间 < 10%

THEN
清理临时文件
 ↓
检查磁盘
 ↓
仍然 < 10%
 ↓
创建告警

或者：

Agent 版本 < 1.5.0
        ↓
自动升级
        ↓
验证
        ↓
失败 → 告警

这是以后非常重要的能力。

十二、⑪ Event / Alert / Incident ⭐⭐⭐⭐⭐

建议不要只有简单 Alert。

做成：

Event
 ↓
Rule
 ↓
Alert
 ↓
Incident
 ↓
Response
 ↓
Recovery

例如：

Defender Threat
Agent Offline
FIM Change
Disk Full
Software Install Failed
Agent Upgrade Failed
Policy Violation
        ↓
      Event
        ↓
      Alert
        ↓
    Incident

然后可以直接：

从告警进入远程桌面。

这个体验非常重要。

十三、⑫ Audit 审计 ⭐⭐⭐⭐⭐

企业系统必须有。

至少记录：

谁
什么时间
对什么资产
执行了什么操作
操作参数
操作结果

重点审计：

登录
远程桌面
Remote Shell
文件传输
软件安装
Agent 升级
安全策略
终端隔离
批量任务
权限变化
十四、⑬ RBAC 权限 ⭐⭐⭐⭐

继续完善：

Admin
Operator
Viewer

以后变成：

角色
 ↓
权限
 ↓
资源范围

例如：

张三只能管理“越南工厂”资产。

而不是只能：

能管理 / 不能管理。

这就是 Scoped RBAC。

十五、⑭ Dashboard / Report ⭐⭐⭐⭐

最终首页不要只是：

在线设备 84 台。

而应该成为 IT 运维驾驶舱。

例如：

Z-View Dashboard

资产
├── 总资产 84
├── 在线 79
└── 离线 5

Agent
├── 正常 76
├── 异常 3
└── 版本过旧 8

安全
├── 合规 72
├── 风险 12
└── 高风险 3

软件
├── 软件数量
├── 过期软件
└── 部署失败

运维
├── 今日任务
├── 成功率
└── 当前告警
十六、⑮ Linux Agent

这个不是现在第一优先级。

等 Windows 平台稳定后再做。

第一阶段：

Ubuntu
Debian
Rocky / RHEL

提供：

CPU
RAM
Disk
Process
Service
Software
Network
Shell
File
Security
Remote management

这样 Z-View 就从：

Windows Endpoint Platform

升级为：

Cross-platform Endpoint Management Platform

十七、我建议明确砍掉的东西

这个很重要。

❌ 永久从产品主线删除
VMware / vCenter

不做。

网络设备

不做。

Zabbix

不做。

自研病毒库

不做。

自研杀毒引擎

不做。

完整 EDR

暂时不做。

Kubernetes / 微服务

现在也不要为了“看起来高级”去做。

你目前规模下：

模块化单体 + 独立 Remote Gateway + Agent

完全够用。

最终优先级

如果让我现在直接给你排开发路线，我会这么排：

🔴 P0：现在马上解决
1. Remote Desktop 性能
Capture
Encode
Queue
Transport
Decode
Render
Input

把卡顿问题彻底定位。

2. Agent 安全
每 Agent 独立身份
Named Pipe fail-closed
Helper 权限边界
升级签名验证
3. Job Center

把各种 Agent 操作统一。

4. Audit

所有高风险操作可追溯。

🟠 P1：下一阶段核心
Agent Fleet
Policy Engine
Software Management
Patch Management
Event / Alert / Incident
Scoped RBAC
Dashboard

这一阶段完成以后，我认为：

Z-View 才真正成为一个完整的企业终端管理平台。

🟡 P2：平台化
Automation
Remote Shell
高级 Remote Desktop
安全基线
合规检查
资产生命周期
软件仓库
批量任务
任务编排
🟢 P3：扩展
Linux Agent
更多自动化
AI 运维助手
智能故障诊断
异常检测
自动修复
最终我给 Z-View 定的产品边界
                         Z-VIEW
                           │
              Enterprise Endpoint Platform
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
      ASSET              MANAGE             REMOTE
        │                  │                  │
       CMDB             Agent Fleet       Desktop
       Hardware         Software           Shell
       Software         Patch              File
       User             Policy             Clipboard
       Lifecycle        Job
        │                  │
        └──────────────────┼──────────────────┘
                           │
                     SECURITY
                           │
              Defender / Firewall / USB
              Process / FIM / Isolation
                           │
                     AUTOMATION
                           │
                  Event → Job → Verify
                           │
                       AUDIT
                           │
                   RBAC / Compliance